spring:
  devtools:
    restart:
      aditional-paths: src/main/java
  application:
    name: backend-estudiantes-service
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    plataform: postgres
    driver-class-name: org.postgresql.Driver
    dbname: Estudiantes
    url: jdbc:postgresql://localhost:5432/estudiantes
    username: postgres
    password: postre
    data: classpath:EstudiantesData.sql
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081